{
  "name": "@ls/vscode-companion",
  "displayName": "LS CLI for VS Code",
  "description": "Enable LS CLI with direct access to your IDE workspace.",
  "version": "0.42.0-nightly.20260428.g59b2dea0e",
  "publisher": "ls",
  "icon": "assets/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/ls-cli/ls-cli.git",
    "directory": "packages/vscode-ide-companion"
  },
  "engines": {
    "vscode": "^1.99.0"
  },
  "license": "LICENSE",
  "preview": true,
  "categories": [
    "AI"
  ],
  "keywords": [
    "ls-cli",
    "ls cli",
    "ai",
    "cli",
    "ide integration",
    "ide companion"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "configuration": {
      "title": "LS",
      "properties": {
        "ls.debug.logging.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable detailed logging for debugging the LS CLI Companion."
        }
      }
    },
    "languages": [
      {
        "id": "ls-diff-editable"
      }
    ],
    "commands": [
      {
        "command": "ls.diff.accept",
        "title": "LS CLI: Accept Diff",
        "icon": "$(check)"
      },
      {
        "command": "ls.diff.cancel",
        "title": "LS CLI: Close Diff Editor",
        "icon": "$(close)"
      },
      {
        "command": "ls-cli.runLSCLI",
        "title": "LS CLI: Run"
      },
      {
        "command": "ls-cli.showNotices",
        "title": "LS CLI: View Third-Party Notices"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "ls.diff.accept",
          "when": "ls.diff.isVisible"
        },
        {
          "command": "ls.diff.cancel",
          "when": "ls.diff.isVisible"
        }
      ],
      "editor/title": [
        {
          "command": "ls.diff.accept",
          "when": "ls.diff.isVisible",
          "group": "navigation"
        },
        {
          "command": "ls.diff.cancel",
          "when": "ls.diff.isVisible",
          "group": "navigation"
        }
      ]
    },
    "keybindings": [
      {
        "command": "ls.diff.accept",
        "key": "ctrl+s",
        "when": "ls.diff.isVisible"
      },
      {
        "command": "ls.diff.accept",
        "key": "cmd+s",
        "when": "ls.diff.isVisible"
      }
    ]
  },
  "main": "./dist/extension.cjs",
  "type": "module",
  "scripts": {
    "prepackage": "npm run generate:notices && npm run check-types && npm run lint && npm run build:prod",
    "build": "npm run build:dev",
    "build:dev": "node esbuild.js",
    "build:prod": "node esbuild.js --production",
    "generate:notices": "node ./scripts/generate-notices.js",
    "prepare": "npm run generate:notices",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "watch": "npm-run-all2 -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "vsce package --no-dependencies",
    "test": "vitest run",
    "test:ci": "vitest run --coverage",
    "validate:notices": "node ./scripts/validate-notices.js"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "20.x",
    "@types/vscode": "^1.99.0",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "@vscode/vsce": "^3.6.0",
    "esbuild": "^0.25.3",
    "eslint": "^9.25.1",
    "npm-run-all2": "^8.0.2",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.23.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "zod": "^3.25.76"
  }
}
